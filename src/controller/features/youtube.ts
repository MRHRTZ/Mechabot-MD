import { AnyWASocket } from "@adiwajshing/baileys";
import { MessageMaterial } from "../../lib/Types/ProcessWebMessageTypes";

export default async function instagram(sock: AnyWASocket, m: MessageMaterial) {
    
    
}